apiVersion: v1
kind: Service
metadata:
   name: user-service
   labels:
        app: k8s-test
        tier: backend
        type: service
spec:
  type: NodePort
  selector: 
    app: k8s-test
    tier: backend
    type: service
  ports:
    - port: 9091
      targetPort: 8080      
    